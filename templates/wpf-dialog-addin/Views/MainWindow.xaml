<Window x:Class="InvAddin.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvAddin.Views"
        mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200"
            Height="300" Width="300">
    <DockPanel>
        <!--Close & Save Buttons-->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center">
            <Label x:Name="GreetingLabel" Content="{Binding Path=Greeting}"/>
            <Button x:Name="CancelButton" Click="CancelButton_Click" Content="Close"/>
        </StackPanel>
    </DockPanel>
</Window>